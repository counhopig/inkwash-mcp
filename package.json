{
  "name": "inkpaper-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server exposing the Inkpaper server's webhook channels so any agent (opencode, Claude Code, scripts) can push notifications to the Inkpaper e-ink device",
  "scripts": {
    "start": "bun run src/index.ts",
    "test": "bun run test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.19.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0"
  }
}